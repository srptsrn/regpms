<table class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th nowrap style="width:90px;"><%= PositionCapacityGroup.human_attribute_name :sorting %></th>
            <th nowrap><%= PositionCapacityGroup.human_attribute_name :name %></th>
            <% unless action_name == "show" %>
                <th style="width:30px;">
                    <%= link_to_add_association f, :position_capacity_groups, partial: "/settings/position_capacity_groups/nested_fields", class: "btn btn-sm btn-success",
                    "data-association-insertion-node" => "#position_capacity_group_fields",
                    "data-association-insertion-method" => :append do %>
                    <i class='fa fa-plus'></i>
                    <% end %>
                </th>
            <% end %>
        </tr>
    </thead>
    <tbody id="position_capacity_group_fields">
        <%= f.simple_fields_for :position_capacity_groups do |position_capacity_group| %>
            <%= render '/settings/position_capacity_groups/nested_fields', :f => position_capacity_group %>
        <% end %>
    </tbody>
</table>
<script type="text/javascript">
    jQuery(function($) {
        $('#position_capacity_group_fields').on('cocoon:after-insert', function(e, insertedItem) {
            init_input_date_picker();
            init_input_time_picker();
            init_input_datetime_picker();
            init_input_datetime_picker();
        });
    });
</script>