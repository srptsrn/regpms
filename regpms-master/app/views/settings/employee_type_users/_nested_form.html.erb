<table class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th nowrap><%= EmployeeTypeUser.human_attribute_name :employee_type %></th>
            <th nowrap><%= EmployeeTypeUser.human_attribute_name :user %></th>
            <th nowrap><%= EmployeeTypeUser.human_attribute_name :evaluation %></th>
            <% unless action_name == "show" %>
                <th style="width:30px;">
                    <%= link_to_add_association f, :employee_type_users, partial: "/settings/employee_type_users/nested_fields", class: "btn btn-sm btn-success",
                    "data-association-insertion-node" => "#employee_type_user_fields",
                    "data-association-insertion-method" => :append do %>
                    <i class='fa fa-plus'></i>
                    <% end %>
                </th>
            <% end %>
        </tr>
    </thead>
    <tbody id="employee_type_user_fields">
        <%= f.simple_fields_for :employee_type_users do |employee_type_user| %>
            <%= render '/settings/employee_type_users/nested_fields', :f => employee_type_user %>
        <% end %>
    </tbody>
</table>
<script type="text/javascript">
    jQuery(function($) {
        $('#employee_type_user_fields').on('cocoon:after-insert', function(e, insertedItem) {
            init_input_date_picker();
            init_input_time_picker();
            init_input_datetime_picker();
        });
    });
</script>