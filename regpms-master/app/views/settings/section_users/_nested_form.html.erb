<table class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th nowrap><%= SectionUser.human_attribute_name :user %></th>
            <% unless action_name == "show" %>
                <th style="width:30px;">
                    <%= link_to_add_association f, :section_users, partial: "/settings/section_users/nested_fields", class: "btn btn-sm btn-success",
                    "data-association-insertion-node" => "#section_user_fields",
                    "data-association-insertion-method" => :append do %>
                    <i class='fa fa-plus'></i>
                    <% end %>
                </th>
            <% end %>
        </tr>
    </thead>
    <tbody id="section_user_fields">
        <%= f.simple_fields_for :section_users do |section_user| %>
            <%= render '/settings/section_users/nested_fields', :f => section_user %>
        <% end %>
    </tbody>
</table>
<script type="text/javascript">
    jQuery(function($) {
        $('#section_user_fields').on('cocoon:after-insert', function(e, insertedItem) {
            init_input_date_picker();
            init_input_time_picker();
            init_input_datetime_picker();
        });
    });
</script>