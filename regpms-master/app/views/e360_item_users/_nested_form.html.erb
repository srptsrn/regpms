<table class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th nowrap><%= E360ItemUser.human_attribute_name :e360_user %></th>
            <th nowrap><%= E360ItemUser.human_attribute_name :e360_item %></th>
            <th nowrap><%= E360ItemUser.human_attribute_name :score %></th>
            <% unless action_name == "show" %>
                <th style="width:30px;">
                    <%= link_to_add_association f, :e360_item_users, partial: "/e360_item_users/nested_fields", class: "btn btn-sm btn-success",
                    "data-association-insertion-node" => "#e360_item_user_fields",
                    "data-association-insertion-method" => :append do %>
                    <i class='fa fa-plus'></i>
                    <% end %>
                </th>
            <% end %>
        </tr>
    </thead>
    <tbody id="e360_item_user_fields">
        <%= f.simple_fields_for :e360_item_users do |e360_item_user| %>
            <%= render '/e360_item_users/nested_fields', :f => e360_item_user %>
        <% end %>
    </tbody>
</table>
<script type="text/javascript">
    jQuery(function($) {
        $('#e360_item_user_fields').on('cocoon:after-insert', function(e, insertedItem) {
            init_input_date_picker();
            init_input_time_picker();
            init_input_datetime_picker();
        });
    });
</script>